# 修仙系统 + 交易系统 项目知识库

> 本文件用于跨会话知识传递。新建会话时，请先读取本文件了解项目全貌，再针对具体需求修改。

---

## 一、项目概览

有两个子系统，位于不同位置：

1. **游戏端修仙插件**：`C:\mc\plugins\xiuxian\xiuxian.js`（单文件，约 3820 行）
2. **网页交易平台**：`C:\mc\xiuxian_ctm\`（前端 + 后端）

---

## 二、游戏端修仙插件（xiuxian.js）

### 运行环境
- 框架：LegacyScriptEngine (LSE) + QuickJS（单文件插件）
- 玩法：境界突破、灵根、灵石、炼丹、妖丹、储物袋、飞行、头顶称号
- 参考 skill：`C:\mc\.agents\skills\lse-skill\`（API 文档）

### 核心数据结构
- **`DATA`**（KVDatabase）：玩家修仙数据，key=xuid，存境界/修为/灵石/灵根/五行等
- **`STORAGE_BAG`**（KVDatabase + 内存缓存 `_bagMemory`）：储物袋数据 `{level, used, slots}`
- **`CTM_INCOME` / `CTM_STASH`**（KVDatabase）：交易相关（当前主要靠网页存）

### 重要常量
- `REALMS`：12 境界；`BREAK_COST`：突破所需修为
- `DAN_RECIPES`：6 种丹药配方（huiling/peiyuan/wuxing/xuming/pozhang/xisui）
- `DAN_QUALITY_RATE`：丹药品质概率（**注意：rate 是炼丹品质概率，不是丹药效果成功率！**）
- `XISUI_RATE`：洗髓丹按品阶的成功率（一品10% ~ 九品95%，独立于 DAN_QUALITY_RATE）
- `CN_MATERIALS`：药材中文名映射；`FLOWER_TYPES`：花列表
- `CROP_QUALITY_RATE` / `FLOWER_QUALITY_RATE`：药材品质概率

### 颜色规范（UI）
- 青§b 绿§a 黄§e 金§6 红§c 粉§d
- **表单/按钮/表格禁用灰色 §7**（看不清）
- 五行颜色：金§e 木§a 水§b 火§c 土§6（ELEMENT_COLORS）

### 修仙物品 NBT 标记（Lore）
- 妖丹用粘液球 slime_ball，标记 `§8xiuxian:<生物id>`
- 丹药用苹果 apple，标记 `§8xiuxian:danyao` + `§8xiuxian:key:xxx` + `§8xiuxian:quality:X`
- 药材标记 `§8xiuxian:herb` + `§8xiuxian:quality:X`
- 储物袋用荧光墨囊 glow_ink_sac，标记 `§8xiuxian:storage_bag` + `§8xiuxian:bag_level:X`
- **五行丹**：额外 `§8xiuxian:element:X`（随机绑定金木水火土）
- 功能判定靠这些标记，玩家改物品名（anvil）只能改显示名改不了 Lore，**无法仿冒修仙物品**

### 统一物品格式（重要）
所有修仙物品 Lore 属性行统一 **`标签：值`** 格式，每行一个：
- 药材：`品质：X`、`类别：基础药材/稀有药料`
- 妖丹：`灵石：X`、`属性：X`、`属性值：X`
- 储物袋：`等级：X`、`空间：X格`
- 丹药：`品阶：X品`、`效果：XXX`

### 储物袋系统
- 等级 1-10，每级 +30 格，最高 300 格
- 存带 NBT 物品（只认修仙标记或无严格限制）
- 强化：消耗 1 个 1-3 级储物袋，成功率 50%
- 强化「手中」：直接改手持储物袋 NBT 升级（方案B）
- **强化已移除确认页**：选目标后直接强化（单纯流程）
- 储物袋可被存入另一个储物袋（无套娃问题，数据每玩家一份）

### 炼丹系统要点
- 药材品质分农作物（1-5级）/花（1-9级），影响炼丹成功率/丹药品质
- 炼制需「同品质药材」，数量不足提示
- 炼丹有「炼制失败」（消耗材料）+ 丹药各有「效果」
- 洗髓丹：品阶越高成功率越高 + 洗出的灵根品质越好（randomRootByQuality）
- 破障丹：固定九品，突破+15%（一次性）
- ≥6品丹药炼制成功 → 全服 Toast 广播

### 交易相关（游戏端）
- 配置：`CTM_API_URL`（默认 http://localhost:3000）、`CTM_SERVER_KEY`
- `ctmRequest()`：HTTP 请求封装（注意：**非 200 状态码也要把响应体传回**，否则 409 等错误信息丢失）
- `/ctm` 命令：用 `setEnum('action', ...)` 子命令注册（参考 /xx 写法）
- 仅修仙物品可上架（Lore 含 §8xiuxian: 标记）

### 已知问题 / 需注意
- **`/ctm` 命令注册（最终定稿）**：**不要用 `setEnum`/BDS 枚举系统** —— 在 LSE 0.19 上枚举匹配反复出问题（枚举值与插件命令冲突、optional 枚举不稳、空 overload 与带参 overload 共存，均报「语法错误」或部分子命令失效，`/xx` 5 个枚举恰好正常但 `/ctm` 9-10 个就不行）。正解：`mandatory('action', ParamType.String)` + `optional('args', ParamType.RawText)` + overload 只留 `['action']`、`['action','args']` 两个，回调内自行比对 action 字符串。菜单入口 `/ctm cd`（无参 `/ctm` 会报语法错误并显示 usage）。已知冲突词：`pay` 撞 LegacyMoney 的 `/pay`（已弃用，用 `buy`）。若报「此世界未启用无敌模式」或「意外的 xxx」同理换关键字
- **命令改动必须完整重启 BDS**：`ll reload` 后命令回调会挂在已销毁的 JS 引擎上，执行报 `get on empty Global` 崩溃；且旧命令残留导致新注册被拒（日志：`运行时命令 xxx 已存在，除 setOverload 外的更改不会被应用`）。症状混乱（部分子命令可用、部分语法错误）时先检查是否 reload 过、日志有无上述警告
- `ll reload` 不触发 onServerStarted → **命令改动需重启 BDS 才生效**
- `player.getHand()` 返回手持物品引用，可用于排除手持槽位（isHandSlotItem）
- 无 `getHandSlot` API（不存在），需遍历或用引用比较
- 无 `emptySlotCount` 属性（不存在）
- 矿石挖矿得灵石；极小概率额外灵石

### 游戏端指令
- `/xiuxian` 主菜单；`/xx <action>` 各种操作
- `/cun` 存手持物品；`/qh` 强化手中储物袋
- `/ctmgive`（OP）获取所有丹药+药材
- `/ctm` 交易系统（参考 /xx 写法注册）

---

## 三、网页交易平台（xiuxian_ctm）

### 技术栈
- 后端：Node.js + Express + **node:sqlite**（Node 内置，零依赖）
- 前端：Vue3 + Vite + Pinia + Vue Router
- 像素风 UI；本地像素字体（方舟像素字体 font 在 web/public/fonts/）

### 目录结构
```
server/
  app.js          后端入口（端口 3000）
  db.js           SQLite 初始化（库：server/data.db）
  auth-middleware.js  serverKey + JWT 鉴权
  routes/
    auth.js       注册/登录/绑定/改密
    items.js      商品/上架/下架/取回
    cart.js       购物车
    orders.js     支付/订单/收入
    profiles.js   玩家修仙信息上报/查询
web/
  vite.config.js  端口5173，/api 代理到 3000
  src/
    api.js        API 封装
    store.js      Pinia 登录态
    router.js
    views/        ItemList 市场 / Login / Register / Cart / MyItems / Orders
    styles/pixel.css
```

### 数据库表（server/data.db）
- `servers`：每服密钥（id/secret/cross_trade 跨服开关）
- `users`：账号（username/password_hash/xuid/server_id）
- `items`：商品（含 item_data 完整 NBT；status: on_sale/sold/off；deleted 软删）
- `cart`：购物车（多人可加同一商品）
- `orders`：订单
- `seller_income`：卖家收入（待领取）
- `pickups`：下架待取回
- `player_profiles`：玩家修仙信息上报

### 核心业务逻辑
- **每玩家一份储物袋数据**，跨服按 serverKey 区分
- **跨服交易开关**：`servers.cross_trade`（0关/1开，默认关）。游戏端 `/ctm cross` 查询状态、`/ctm cross on|off` 开关（**仅 OP**，`player.isOP()` 判断），走后端 `GET/POST /api/settings/cross-trade`（serverKey 鉴权）。网页市场页的「跨服交易」勾选框只控制浏览范围，购买权限由 cross_trade 决定
- 购买：网页加购物车 → 游戏 `/ctm pay` 扣灵石 → 后端扣库存返回 SNBT → 游戏发物品
- 防超卖：`UPDATE ... WHERE remaining >= ?` 原子扣减
- 卖家收入：支付时自动记入 seller_income，游戏 `/ctm claim` 领取
- 下架取回：网页下架 → pickups 表 → 游戏 `/ctm back` 取回剩余

### API 接口（基础路径 /api）
- `POST /auth/register|login|bind|reset-password`
- `POST /items`（上架）、`GET /items`（列表，`serverId` 空=全部）、`GET /items/seller/:user`、`POST /items/:id/off|delete`
- `GET/POST/DELETE /cart`
- `GET /orders/my-bought|my-sold`、`POST /orders/pay`、`GET /orders/income-pending/:user`、`POST /orders/income-claim`
- `GET /items/pickup/:user`、`POST /items/pickup-restore`
- `POST /profiles/report`、`GET /profiles/:username`

### 鉴权
- 游戏端请求：`X-Server-Key` 头（服务器密钥）
- 网页请求：`Authorization: Bearer <JWT>`

### 启动方式
- 后端：`cd server && npm start`（端口 3000）
- 前端：`cd web && npm run dev`（5173）或 `npm run build`

### 已知问题
- `getProfile` 上报时境界颜色用十六进制（避免 § 编码成 Â§ 乱码）
- 前端 `splitTitle`：优先按「XX：冒号」拆名字/属性，无冒号按空格拆

---

## 四、交易系统"三处"一致性要求

交易涉及游戏端、后端、前端，修改功能时注意同步：
1. **游戏端**（xiuxian.js）：指令 + 数据操作
2. **后端**（server/routes/）：API + SQLite
3. **前端**（web/src/）：展示 + 调用

一个功能改动往往要三端配合，例如：
- 新增商品类型 → 前端图标配色 + 游戏端上架校验
- 改支付流程 → 后端接口 + 游戏端 ctmPay

---

## 五、常用排障经验

1. **命令不生效** → 命令在 onServerStarted 注册，`ll reload` 不触发，**需重启 BDS**
2. **命令报「此世界未启用无敌模式」** → setEnum 子命令 / enum 值可能与 BDS 原版冲突，参考 /xx 写法调整或换关键字
3. **物品 NBT 丢失** → 修改物品数量/部分消耗时，用 NBT 重建（getNbt→setTag Count→mc.newItem(nbt)）而非 mc.newItem(type, n)
4. **功能判「已满/不存在」但实际有** → 检查是否用了 LSE 不存在的 API（如 emptySlotCount、getHandSlot、s.setHand）
5. **reload 后 storage 读旧值** → KVDatabase 用内存缓存封装（_bagMemory）规避
6. **前端看不到商品** → 检查后端是否启动（ECONNREFUSED）、serverId 空时列表查全部
7. **上架扣物后物品丢失/没归还** → `player.getHand()` 返回物品引用，先移除背包再 `giveItem(hand)` 归还会因引用失效给 0 个。上架流程必须「先请求后端成功，再从背包移除物品」（失败不扣物）

---

## 六、LSE 关键 API 备忘

- `network.httpGet/httpPost`：发起 HTTP 请求
- `mc.newCommand` + `setEnum/mandatory/optional/overload`：注册命令
- `mc.regPlayerCmd`：简单命令注册（reload 有 version mismatch 问题，慎用）
- `item.getNbt()` / `item.setNbt(nbt)` / `NBT.parseSNBT(snbt)` / `nbt.toSNBT()`：物品 NBT
- `player.getHand()`：手持物品（引用，可操作）
- `player.setHand` 不存在；`mc.newItem(type, count, nbt)` 用法
- `pl.rename(name)`：改头顶显示名（玩家的 CustomName）
- `Confdb.set/get` 用 `listKey()` 遍历（**没有 forEach**）
